apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: project-vxy
spec:
  type: ClusterIP
  selector:
    app: project-vxy
  ports:
    - name: project-vxy-frontend
      port: 8080
      protocol: TCP
      targetPort: 3200
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: project-vxy
spec:
  type: ClusterIP
  selector:
    app: project-vxy
  ports:
    - name: project-vxy-backend
      port: 8080
      protocol: TCP
      targetPort: 3300